---
phase: 03-cross-platform-deployment-and-health
plan: 05
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-03"
  - "03-04"
files_modified:
  - deploy/systemd/transcendence-memory-backend.service
  - deploy/systemd/transcendence-memory-backend.env.example
  - deploy/systemd/README.md
  - src/transcendence_memory/deploy/systemd.py
  - tests/unit/test_systemd_render.py
autonomous: true
requirements:
  - BACK-02
  - BACK-03
user_setup: []
must_haves:
  truths:
    - Linux has a documented native `systemd` deployment path for the backend service
    - Systemd assets are rendered deterministically and reuse the backend launch contract
    - Rerunning the Linux install path preserves working directory and env-file conventions
  artifacts:
    - deploy/systemd/transcendence-memory-backend.service
    - deploy/systemd/transcendence-memory-backend.env.example
    - deploy/systemd/README.md
    - src/transcendence_memory/deploy/systemd.py
    - tests/unit/test_systemd_render.py
  key_links:
    - the service unit references the committed backend runtime entrypoint and env file
    - the README and render helpers use the same unit/env names and paths
---

<objective>
Add the Linux-native systemd deployment alternative and the minimal documentation required to follow it.

Purpose: Phase 3 explicitly promises a documented Linux `systemd` alternative to Docker.
Output: systemd unit/env assets, render helper, and Linux deployment reference/tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md
@.planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md
@src/transcendence_memory/backend/main.py
@src/transcendence_memory/deploy/health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add systemd render helpers and service/env templates</name>
  <files>src/transcendence_memory/deploy/systemd.py, deploy/systemd/transcendence-memory-backend.service, deploy/systemd/transcendence-memory-backend.env.example, tests/unit/test_systemd_render.py</files>
  <read_first>src/transcendence_memory/backend/main.py, .planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md, .planning/phases/03-cross-platform-deployment-and-health/03-VALIDATION.md</read_first>
  <action>Create `src/transcendence_memory/deploy/systemd.py` and add deterministic rendering logic for a Linux service unit plus env file. Create `deploy/systemd/transcendence-memory-backend.service` with concrete `WorkingDirectory=`, `EnvironmentFile=`, `ExecStart=`, `ExecStop=`, and `Restart=on-failure`. Create `deploy/systemd/transcendence-memory-backend.env.example` with backend bind, database, and provider config placeholders. Add `tests/unit/test_systemd_render.py` that asserts those exact unit directives and file names are preserved.</action>
  <verify>rg -n "WorkingDirectory=|EnvironmentFile=|ExecStart=|ExecStop=|Restart=on-failure" deploy/systemd/transcendence-memory-backend.service src/transcendence_memory/deploy/systemd.py tests/unit/test_systemd_render.py</verify>
  <acceptance_criteria>
    - `deploy/systemd/transcendence-memory-backend.service` contains `WorkingDirectory=`
    - `deploy/systemd/transcendence-memory-backend.service` contains `EnvironmentFile=`
    - `deploy/systemd/transcendence-memory-backend.service` contains `Restart=on-failure`
    - `tests/unit/test_systemd_render.py` contains `ExecStart=`
  </acceptance_criteria>
  <done>The Linux systemd path has real service assets and test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Linux systemd path with exact status and log commands</name>
  <files>deploy/systemd/README.md, deploy/systemd/transcendence-memory-backend.service</files>
  <read_first>deploy/systemd/README.md, deploy/systemd/transcendence-memory-backend.service, .planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md</read_first>
  <action>Create `deploy/systemd/README.md` with a minimal but concrete Linux deployment reference: where to place the unit file, where to place the env file, how to run `systemctl daemon-reload`, how to enable/start the service, how to check `systemctl status transcendence-memory-backend`, and how to inspect `journalctl -u transcendence-memory-backend -n 100 --no-pager`. Keep the scope to deployment and health only; do not expand into Phase 5 release docs.</action>
  <verify>rg -n "systemctl daemon-reload|systemctl status transcendence-memory-backend|journalctl -u transcendence-memory-backend -n 100 --no-pager" deploy/systemd/README.md</verify>
  <acceptance_criteria>
    - `deploy/systemd/README.md` contains `systemctl daemon-reload`
    - `deploy/systemd/README.md` contains `systemctl status transcendence-memory-backend`
    - `deploy/systemd/README.md` contains `journalctl -u transcendence-memory-backend -n 100 --no-pager`
  </acceptance_criteria>
  <done>Linux users have a documented native systemd path before Phase 5 documentation polish.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "WorkingDirectory=|EnvironmentFile=|Restart=on-failure" deploy/systemd/transcendence-memory-backend.service` succeeds
- [ ] `rg -n "systemctl daemon-reload|systemctl status transcendence-memory-backend|journalctl -u transcendence-memory-backend -n 100 --no-pager" deploy/systemd/README.md` succeeds
- [ ] `test -f tests/unit/test_systemd_render.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Linux systemd is a documented and real alternative deployment path
- Unit/env render logic matches the documented service contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-platform-deployment-and-health/03-05-SUMMARY.md`
</output>
