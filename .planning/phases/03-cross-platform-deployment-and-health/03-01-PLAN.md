---
phase: 03-cross-platform-deployment-and-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - src/transcendence_memory/backend/main.py
  - tests/unit/test_deploy_config.py
autonomous: true
requirements:
  - BACK-01
user_setup: []
must_haves:
  truths:
    - The backend has a real container build path, not just source code
    - The backend runtime can be launched consistently by Docker without ad hoc shell commands
    - Deployment config tests exist before compose and CLI deploy logic expand
  artifacts:
    - Dockerfile
    - .dockerignore
    - src/transcendence_memory/backend/main.py
    - tests/unit/test_deploy_config.py
  key_links:
    - Dockerfile starts the backend through the committed ASGI entrypoint
    - deploy config tests validate the generated image/runtime assumptions
---

<objective>
Create the container runtime foundation for backend deployment.

Purpose: Docker-first deployment cannot be credible without a concrete image build path and backend launch contract.
Output: Dockerfile, dockerignore, backend runtime entrypoint, and initial deploy-config test scaffolding.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md
@.planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md
@src/transcendence_memory/backend/app.py
@src/transcendence_memory/backend/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Docker build assets and a concrete backend entrypoint</name>
  <files>Dockerfile, .dockerignore, src/transcendence_memory/backend/main.py</files>
  <read_first>src/transcendence_memory/backend/app.py, src/transcendence_memory/backend/settings.py, .planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md</read_first>
  <action>Create `Dockerfile` that builds the current Python application and starts the backend via a concrete ASGI launch target. Add `.dockerignore` that excludes `.venv`, `.pytest_cache`, `__pycache__`, local planning noise, and git metadata. Create `src/transcendence_memory/backend/main.py` that exposes a stable `app` import target and a simple local runner entrypoint so both Docker and future systemd assets can reference the same backend launch contract.</action>
  <verify>rg -n "uvicorn|transcendence_memory.backend.main:app|CMD|ENTRYPOINT" Dockerfile src/transcendence_memory/backend/main.py && test -f .dockerignore</verify>
  <acceptance_criteria>
    - `Dockerfile` contains `uvicorn`
    - `Dockerfile` contains `transcendence_memory.backend.main:app`
    - `.dockerignore` contains `.venv`
    - `src/transcendence_memory/backend/main.py` contains `app`
  </acceptance_criteria>
  <done>The backend can be built and launched through a committed container/runtime contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add deployment-config test scaffolding for container artifacts</name>
  <files>tests/unit/test_deploy_config.py, Dockerfile, .dockerignore</files>
  <read_first>tests/unit/test_deploy_config.py, Dockerfile, .dockerignore, .planning/phases/03-cross-platform-deployment-and-health/03-VALIDATION.md</read_first>
  <action>Create `tests/unit/test_deploy_config.py` with concrete checks that assert the Dockerfile points at the backend ASGI entrypoint, that `.dockerignore` excludes local virtualenv and cache paths, and that the base deployment artifact names are stable. Keep this test focused on artifact rendering/shape rather than attempting a real image build in unit tests.</action>
  <verify>test -f tests/unit/test_deploy_config.py && rg -n "Dockerfile|dockerignore|backend.main:app" tests/unit/test_deploy_config.py</verify>
  <acceptance_criteria>
    - `tests/unit/test_deploy_config.py` exists
    - `tests/unit/test_deploy_config.py` contains `Dockerfile`
    - `tests/unit/test_deploy_config.py` contains `dockerignore`
  </acceptance_criteria>
  <done>Phase 3 has a test-backed container artifact baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "uvicorn|backend.main:app" Dockerfile src/transcendence_memory/backend/main.py` succeeds
- [ ] `rg -n "\\.venv|\\.pytest_cache" .dockerignore` succeeds
- [ ] `test -f tests/unit/test_deploy_config.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Docker packaging has a stable app entrypoint
- Basic artifact tests exist before compose/CLI layers expand
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-platform-deployment-and-health/03-01-SUMMARY.md`
</output>
