---
phase: 03-cross-platform-deployment-and-health
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - compose.yaml
  - deploy/docker/backend.env.example
  - src/transcendence_memory/deploy/docker.py
  - tests/unit/test_deploy_config.py
  - tests/integration/test_compose_smoke.py
autonomous: true
requirements:
  - BACK-01
  - BACK-03
user_setup: []
must_haves:
  truths:
    - The repository has a Docker-first deployment stack with backend and PostgreSQL services
    - Compose startup uses explicit health checks rather than assuming service readiness
    - Deployment artifacts can be re-rendered without destructive state changes
  artifacts:
    - compose.yaml
    - deploy/docker/backend.env.example
    - src/transcendence_memory/deploy/docker.py
    - tests/integration/test_compose_smoke.py
  key_links:
    - compose.yaml uses health checks and persistent data volumes
    - docker deploy helpers render environment files and detect rerun/no-op conditions
---

<objective>
Add the Docker Compose deployment stack and renderable environment artifacts.

Purpose: Phase 3 needs a concrete Docker-first deployment path that is health-aware and rerun-safe.
Output: Compose stack, env templates, docker deploy helper module, and deployment tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md
@.planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md
@Dockerfile
@src/transcendence_memory/backend/settings.py
@src/transcendence_memory/backend/api/routes/health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a Compose stack with health-aware backend and PostgreSQL services</name>
  <files>compose.yaml, deploy/docker/backend.env.example, tests/integration/test_compose_smoke.py</files>
  <read_first>compose.yaml, Dockerfile, src/transcendence_memory/backend/api/routes/health.py, .planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md</read_first>
  <action>Create `compose.yaml` with at least `backend` and `postgres` services. Configure persistent storage for PostgreSQL, inject env values through `deploy/docker/backend.env.example`, and add explicit health checks for PostgreSQL and the backend. Make the backend depend on PostgreSQL health rather than bare start order. Create `tests/integration/test_compose_smoke.py` as an environment-gated smoke entrypoint that can validate compose config or run a real smoke path when Docker is available.</action>
  <verify>rg -n "healthcheck|depends_on|service_healthy|postgres|backend" compose.yaml deploy/docker/backend.env.example tests/integration/test_compose_smoke.py</verify>
  <acceptance_criteria>
    - `compose.yaml` contains `healthcheck`
    - `compose.yaml` contains `depends_on`
    - `compose.yaml` contains `service_healthy`
    - `deploy/docker/backend.env.example` contains `DATABASE_URL=`
  </acceptance_criteria>
  <done>The repository has a health-aware Compose deployment stack.</done>
</task>

<task type="auto">
  <name>Task 2: Add Docker deployment helpers with rerun-safe render logic</name>
  <files>src/transcendence_memory/deploy/docker.py, tests/unit/test_deploy_config.py</files>
  <read_first>src/transcendence_memory/deploy/docker.py, src/transcendence_memory/backend/settings.py, .planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/deploy/docker.py` with concrete helpers that render deployment env files, resolve a working Compose command, and classify deploy state as create/update/no-op for rerun safety. Extend `tests/unit/test_deploy_config.py` to assert that rendered Docker deployment config includes backend and postgres values and that rerendering the same config produces a no-op-compatible result.</action>
  <verify>rg -n "create|update|no-op|compose|env" src/transcendence_memory/deploy/docker.py tests/unit/test_deploy_config.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/deploy/docker.py` contains `no-op`
    - `src/transcendence_memory/deploy/docker.py` contains `compose`
    - `tests/unit/test_deploy_config.py` contains `create`
    - `tests/unit/test_deploy_config.py` contains `update`
  </acceptance_criteria>
  <done>Docker deployment rendering is deterministic and rerun-aware.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "healthcheck|service_healthy" compose.yaml` succeeds
- [ ] `rg -n "create|update|no-op" src/transcendence_memory/deploy/docker.py` succeeds
- [ ] `test -f deploy/docker/backend.env.example && test -f tests/integration/test_compose_smoke.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Docker deployment is health-aware and rerun-safe
- Compose assets are concrete enough for later CLI deploy commands
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-platform-deployment-and-health/03-02-SUMMARY.md`
</output>
