---
phase: 03-cross-platform-deployment-and-health
plan: 04
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/transcendence_memory/backend/api/routes/health.py
  - src/transcendence_memory/deploy/health.py
  - src/transcendence_memory/cli.py
  - tests/api/test_backend_health_route.py
  - tests/cli/test_backend_health_command.py
autonomous: true
requirements:
  - BACK-04
  - VERI-02
user_setup: []
must_haves:
  truths:
    - Backend health checks distinguish process, database, and deployment-state failures
    - CLI health output provides exact next commands rather than generic warnings
    - Health output is safe for terminal use and redacts secrets
  artifacts:
    - src/transcendence_memory/backend/api/routes/health.py
    - src/transcendence_memory/deploy/health.py
    - src/transcendence_memory/cli.py
    - tests/api/test_backend_health_route.py
    - tests/cli/test_backend_health_command.py
  key_links:
    - CLI backend health reuses the same health classification model as backend route and deploy helpers
    - failure classes map to concrete follow-up commands for Docker and systemd operators
---

<objective>
Implement deep backend health diagnostics and actionable CLI health output.

Purpose: Phase 3 is not done if “healthy” only means “the process exists”. Operators need actionable recovery output.
Output: Deeper health route, deploy-health helper module, CLI `backend health`, and health tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md
@.planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md
@src/transcendence_memory/backend/api/routes/health.py
@src/transcendence_memory/backend/settings.py
@src/transcendence_memory/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen backend health reporting and classify backend readiness states</name>
  <files>src/transcendence_memory/backend/api/routes/health.py, src/transcendence_memory/deploy/health.py, tests/api/test_backend_health_route.py</files>
  <read_first>src/transcendence_memory/backend/api/routes/health.py, src/transcendence_memory/backend/settings.py, .planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/deploy/health.py` and deepen `src/transcendence_memory/backend/api/routes/health.py` so health reporting distinguishes at least these classes: app reachable, database reachable, provider config present, and deployment mode metadata. Add `tests/api/test_backend_health_route.py` covering the richer health payload shape and verifying that no secret values are exposed in health responses.</action>
  <verify>rg -n "database|provider|status|deployment" src/transcendence_memory/backend/api/routes/health.py src/transcendence_memory/deploy/health.py tests/api/test_backend_health_route.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/backend/api/routes/health.py` contains `database`
    - `src/transcendence_memory/backend/api/routes/health.py` contains `provider`
    - `src/transcendence_memory/deploy/health.py` contains health classification logic
    - `tests/api/test_backend_health_route.py` contains `provider`
  </acceptance_criteria>
  <done>The backend can report meaningful health states rather than a shallow “ok”.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend health CLI with exact recovery commands</name>
  <files>src/transcendence_memory/cli.py, src/transcendence_memory/deploy/health.py, tests/cli/test_backend_health_command.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/deploy/health.py, .planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md</read_first>
  <action>Extend `src/transcendence_memory/cli.py` with `backend health`. Make it call the deploy health helper and emit exact recovery commands for each failure class, including `docker compose ps`, `docker compose logs backend --tail=100`, `systemctl status transcendence-memory-backend`, and `journalctl -u transcendence-memory-backend -n 100 --no-pager` where relevant. Add `tests/cli/test_backend_health_command.py` asserting those exact strings appear for the matching failure states and that success output remains concise and secret-safe.</action>
  <verify>rg -n "backend health|docker compose ps|docker compose logs backend --tail=100|systemctl status transcendence-memory-backend|journalctl -u transcendence-memory-backend -n 100 --no-pager" src/transcendence_memory/cli.py src/transcendence_memory/deploy/health.py tests/cli/test_backend_health_command.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `backend health`
    - `tests/cli/test_backend_health_command.py` contains `docker compose ps`
    - `tests/cli/test_backend_health_command.py` contains `systemctl status transcendence-memory-backend`
    - `tests/cli/test_backend_health_command.py` contains `journalctl -u transcendence-memory-backend -n 100 --no-pager`
  </acceptance_criteria>
  <done>CLI health diagnostics provide exact operator commands instead of vague guidance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "database|provider" src/transcendence_memory/backend/api/routes/health.py` succeeds
- [ ] `rg -n "backend health|docker compose ps|systemctl status transcendence-memory-backend" src/transcendence_memory/cli.py tests/cli/test_backend_health_command.py` succeeds
- [ ] `test -f tests/api/test_backend_health_route.py && test -f tests/cli/test_backend_health_command.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Health reporting is deeper than process-alive checks
- CLI failure output gives exact next commands
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-platform-deployment-and-health/03-04-SUMMARY.md`
</output>
