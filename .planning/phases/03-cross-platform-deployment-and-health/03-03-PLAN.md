---
phase: 03-cross-platform-deployment-and-health
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/transcendence_memory/cli.py
  - src/transcendence_memory/deploy/docker.py
  - tests/cli/test_backend_deploy_command.py
  - tests/integration/test_compose_smoke.py
autonomous: true
requirements:
  - BACK-01
  - BACK-03
user_setup: []
must_haves:
  truths:
    - The CLI can deploy the backend through the Docker-first path on supported platforms
    - Rerunning backend deployment is idempotent and avoids destructive defaults
    - The CLI exposes exact follow-up commands after deploy actions
  artifacts:
    - src/transcendence_memory/cli.py
    - src/transcendence_memory/deploy/docker.py
    - tests/cli/test_backend_deploy_command.py
  key_links:
    - backend deploy CLI reuses the deploy helper module instead of shelling arbitrary commands inline
    - rerun logic distinguishes create, update, and no-op behavior
---

<objective>
Add the CLI deployment surface for Docker-backed backend rollout and rerun-safe redeploys.

Purpose: Users need a canonical operator command, not just deployment files on disk.
Output: `backend deploy` / `backend restart` CLI behavior, deploy tests, and rerun-safe command paths.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md
@.planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md
@src/transcendence_memory/cli.py
@src/transcendence_memory/deploy/docker.py
@compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend deploy and restart commands to the CLI</name>
  <files>src/transcendence_memory/cli.py, tests/cli/test_backend_deploy_command.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/deploy/docker.py, .planning/phases/03-cross-platform-deployment-and-health/03-CONTEXT.md</read_first>
  <action>Extend `src/transcendence_memory/cli.py` with a `backend` command group and add concrete commands `backend deploy` and `backend restart`. `backend deploy` must render deployment assets, report whether the operation is create/update/no-op, and run the Docker Compose path without destructive cleanup defaults. `backend restart` must restart only the backend service, not destroy volumes. Create `tests/cli/test_backend_deploy_command.py` covering create, update, and no-op messaging plus the exact next-step commands emitted after deployment.</action>
  <verify>rg -n "backend deploy|backend restart|create|update|no-op" src/transcendence_memory/cli.py tests/cli/test_backend_deploy_command.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `backend`
    - `src/transcendence_memory/cli.py` contains `deploy`
    - `src/transcendence_memory/cli.py` contains `restart`
    - `tests/cli/test_backend_deploy_command.py` contains `no-op`
  </acceptance_criteria>
  <done>The CLI exposes a first-class Docker deployment surface with rerun-safe semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Make Docker deploy helpers execute compose commands and preserve data on rerun</name>
  <files>src/transcendence_memory/deploy/docker.py, tests/integration/test_compose_smoke.py</files>
  <read_first>src/transcendence_memory/deploy/docker.py, compose.yaml, deploy/docker/backend.env.example, .planning/phases/03-cross-platform-deployment-and-health/03-RESEARCH.md</read_first>
  <action>Extend `src/transcendence_memory/deploy/docker.py` with the concrete command execution path for `docker compose up -d` / restart behavior and with safe handling of already-present deployment state. Ensure the helper preserves data volumes on rerun and returns exact suggested follow-up commands like `docker compose ps` and `docker compose logs backend --tail=100`. Extend the compose smoke test entrypoint so it can validate CLI-driven compose command composition when Docker is available.</action>
  <verify>rg -n "docker compose up -d|docker compose ps|docker compose logs backend --tail=100|restart" src/transcendence_memory/deploy/docker.py tests/integration/test_compose_smoke.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/deploy/docker.py` contains `docker compose up -d`
    - `src/transcendence_memory/deploy/docker.py` contains `docker compose ps`
    - `src/transcendence_memory/deploy/docker.py` contains `docker compose logs backend --tail=100`
    - `tests/integration/test_compose_smoke.py` contains `compose`
  </acceptance_criteria>
  <done>Docker deploy helpers execute concrete operator commands and preserve data across reruns.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "backend deploy|backend restart" src/transcendence_memory/cli.py` succeeds
- [ ] `rg -n "docker compose up -d|docker compose ps" src/transcendence_memory/deploy/docker.py` succeeds
- [ ] `test -f tests/cli/test_backend_deploy_command.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- CLI deployment is canonical and rerun-safe
- Post-deploy guidance contains concrete next commands
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-platform-deployment-and-health/03-03-SUMMARY.md`
</output>
