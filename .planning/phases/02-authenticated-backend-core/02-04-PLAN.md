---
phase: 02-authenticated-backend-core
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - src/transcendence_memory/backend/auth/oauth.py
  - src/transcendence_memory/backend/auth/tokens.py
  - src/transcendence_memory/cli.py
  - tests/unit/test_oauth_redaction.py
  - tests/integration/test_oauth_cli_flow.py
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
user_setup: []
must_haves:
  truths:
    - OAuth login works through a browser-based native-client flow with PKCE and loopback redirect behavior
    - CLI auth status and logout work for OAuth-backed sessions
    - Refresh tokens and equivalent secrets remain in local secret storage only
  artifacts:
    - src/transcendence_memory/backend/auth/oauth.py
    - src/transcendence_memory/backend/auth/tokens.py
    - src/transcendence_memory/cli.py
    - tests/unit/test_oauth_redaction.py
    - tests/integration/test_oauth_cli_flow.py
  key_links:
    - the CLI auth commands use the same auth state models as backend dependencies
    - OAuth token handling is redacted in status/log output and persisted only through the secret path contract
---

<objective>
Implement the OAuth CLI/native flow with PKCE, loopback callback handling, redacted status output, and logout/reset behavior.

Purpose: Phase 2 must support browser-based OAuth without violating the existing secret-handling rules.
Output: OAuth client helpers, CLI login/status/logout flow, token redaction, and integration coverage.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authenticated-backend-core/02-CONTEXT.md
@.planning/phases/02-authenticated-backend-core/02-RESEARCH.md
@src/transcendence_memory/backend/auth/models.py
@src/transcendence_memory/backend/settings.py
@src/transcendence_memory/bootstrap/persistence.py
@src/transcendence_memory/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement OAuth client helpers and token-state redaction</name>
  <files>src/transcendence_memory/backend/auth/oauth.py, src/transcendence_memory/backend/auth/tokens.py, tests/unit/test_oauth_redaction.py</files>
  <read_first>src/transcendence_memory/backend/auth/models.py, src/transcendence_memory/backend/settings.py, .planning/phases/02-authenticated-backend-core/02-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/backend/auth/oauth.py` and `src/transcendence_memory/backend/auth/tokens.py`. Implement OAuth helpers around Authorization Code + PKCE with a loopback redirect on `127.0.0.1`, not an implicit flow. Add token-state helpers that persist access/refresh token material in secret storage while exposing only redacted metadata to the rest of the app. Add `tests/unit/test_oauth_redaction.py` proving that status serialization and debug output do not contain refresh tokens or raw access tokens.</action>
  <verify>rg -n "PKCE|127.0.0.1|refresh|redacted|token" src/transcendence_memory/backend/auth/oauth.py src/transcendence_memory/backend/auth/tokens.py tests/unit/test_oauth_redaction.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/backend/auth/oauth.py` contains `PKCE`
    - `src/transcendence_memory/backend/auth/oauth.py` contains `127.0.0.1`
    - `src/transcendence_memory/backend/auth/tokens.py` contains `refresh`
    - `tests/unit/test_oauth_redaction.py` contains `redacted`
  </acceptance_criteria>
  <done>OAuth helpers and token redaction behavior exist as reusable code, not route-local logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI login/status/logout and OAuth integration test coverage</name>
  <files>src/transcendence_memory/cli.py, tests/integration/test_oauth_cli_flow.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/backend/auth/oauth.py, src/transcendence_memory/backend/auth/tokens.py, .planning/phases/02-authenticated-backend-core/02-CONTEXT.md</read_first>
  <action>Extend `src/transcendence_memory/cli.py` with concrete `auth login`, `auth status`, and `auth logout` commands for the OAuth path. `auth login` must drive the browser-based loopback flow, `auth status` must show redacted metadata only, and `auth logout` must clear or rotate stored OAuth credentials from the secret store. Add `tests/integration/test_oauth_cli_flow.py` with mocked provider responses that prove login, status, and logout behavior end to end without printing raw token values.</action>
  <verify>rg -n "auth login|auth status|auth logout" src/transcendence_memory/cli.py && test -f tests/integration/test_oauth_cli_flow.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `auth login`
    - `src/transcendence_memory/cli.py` contains `auth logout`
    - `tests/integration/test_oauth_cli_flow.py` contains `login`
    - `tests/integration/test_oauth_cli_flow.py` contains `logout`
  </acceptance_criteria>
  <done>The CLI exposes the full OAuth lifecycle and tests assert the expected flow shape.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "PKCE|127.0.0.1" src/transcendence_memory/backend/auth/oauth.py` succeeds
- [ ] `rg -n "auth login|auth logout" src/transcendence_memory/cli.py` succeeds
- [ ] `test -f tests/unit/test_oauth_redaction.py && test -f tests/integration/test_oauth_cli_flow.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- OAuth uses the native-client browser + PKCE path
- Refresh-token and access-token material remains redacted outside secret storage
</success_criteria>

<output>
After completion, create `.planning/phases/02-authenticated-backend-core/02-04-SUMMARY.md`
</output>
