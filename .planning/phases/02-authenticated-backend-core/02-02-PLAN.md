---
phase: 02-authenticated-backend-core
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - alembic.ini
  - alembic/env.py
  - alembic/versions/0001_phase2_memory_core.py
  - src/transcendence_memory/backend/db/base.py
  - src/transcendence_memory/backend/db/models.py
  - src/transcendence_memory/backend/db/session.py
  - tests/integration/test_pgvector_memory.py
autonomous: true
requirements:
  - BACK-05
user_setup: []
must_haves:
  truths:
    - PostgreSQL + pgvector is implemented as the authoritative persistence layer for memory records
    - The repository has a concrete migration path for the Phase 2 schema
    - Integration tests exist for real vector-backed persistence rather than in-memory placeholders
  artifacts:
    - alembic.ini
    - alembic/env.py
    - alembic/versions/0001_phase2_memory_core.py
    - src/transcendence_memory/backend/db/models.py
    - src/transcendence_memory/backend/db/session.py
    - tests/integration/test_pgvector_memory.py
  key_links:
    - backend DB session code and models line up with the migration and pgvector schema
    - integration tests exercise the same Postgres-backed memory schema used by the backend
---

<objective>
Implement the Phase 2 persistence layer with PostgreSQL + pgvector and a real migration path.

Purpose: The phase cannot satisfy `BACK-05` without real vector-backed persistence.
Output: Database session layer, memory schema, migrations, and pgvector-backed integration tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authenticated-backend-core/02-CONTEXT.md
@.planning/phases/02-authenticated-backend-core/02-RESEARCH.md
@src/transcendence_memory/backend/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLAlchemy session and memory schema with pgvector columns</name>
  <files>src/transcendence_memory/backend/db/base.py, src/transcendence_memory/backend/db/models.py, src/transcendence_memory/backend/db/session.py</files>
  <read_first>src/transcendence_memory/backend/settings.py, .planning/phases/02-authenticated-backend-core/02-RESEARCH.md, .planning/phases/02-authenticated-backend-core/02-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/backend/db/base.py`, `src/transcendence_memory/backend/db/models.py`, and `src/transcendence_memory/backend/db/session.py`. Define a concrete SQLAlchemy base and memory-record schema that stores record ID, content, metadata payload, embedding vector, provider, model, and timestamps. Use the Python pgvector integration rather than a generic array or JSON field. Create a DB engine/session factory that reads the concrete `database_url` setting from the new backend settings model.</action>
  <verify>rg -n "Vector|embedding|provider|model|database_url" src/transcendence_memory/backend/db/models.py src/transcendence_memory/backend/db/session.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/backend/db/models.py` contains `embedding`
    - `src/transcendence_memory/backend/db/models.py` contains `provider`
    - `src/transcendence_memory/backend/db/models.py` contains `model`
    - `src/transcendence_memory/backend/db/session.py` contains `database_url`
  </acceptance_criteria>
  <done>The backend has real SQLAlchemy + pgvector persistence primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Add Alembic migration scaffolding and pgvector integration test</name>
  <files>alembic.ini, alembic/env.py, alembic/versions/0001_phase2_memory_core.py, tests/integration/test_pgvector_memory.py</files>
  <read_first>src/transcendence_memory/backend/db/models.py, .planning/phases/02-authenticated-backend-core/02-VALIDATION.md</read_first>
  <action>Create `alembic.ini`, `alembic/env.py`, and an initial migration file `alembic/versions/0001_phase2_memory_core.py` that creates the Phase 2 memory schema and enables pgvector where required. Add `tests/integration/test_pgvector_memory.py` with concrete integration coverage for inserting embedded records and executing vector-backed similarity search against PostgreSQL. Do not use SQLite or an in-memory stand-in in this test file.</action>
  <verify>test -f alembic.ini && rg -n "vector|embedding|memory" alembic/versions/0001_phase2_memory_core.py tests/integration/test_pgvector_memory.py</verify>
  <acceptance_criteria>
    - `alembic/versions/0001_phase2_memory_core.py` contains `vector` or pgvector setup
    - `alembic/versions/0001_phase2_memory_core.py` contains `embedding`
    - `tests/integration/test_pgvector_memory.py` contains `search`
    - `tests/integration/test_pgvector_memory.py` contains `postgres`
  </acceptance_criteria>
  <done>Migration and persistence verification are real and Postgres-backed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f alembic.ini && test -f alembic/env.py` succeeds
- [ ] `rg -n "embedding|provider|model" src/transcendence_memory/backend/db/models.py` succeeds
- [ ] `rg -n "vector|postgres" tests/integration/test_pgvector_memory.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- PostgreSQL + pgvector are concretely implemented, not deferred
- The repository has a migration path and real persistence integration coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-authenticated-backend-core/02-02-SUMMARY.md`
</output>
