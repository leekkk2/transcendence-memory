---
phase: 02-authenticated-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/transcendence_memory/backend/__init__.py
  - src/transcendence_memory/backend/app.py
  - src/transcendence_memory/backend/settings.py
  - src/transcendence_memory/backend/auth/models.py
  - tests/conftest.py
autonomous: true
requirements:
  - AUTH-01
  - BACK-05
user_setup: []
must_haves:
  truths:
    - The repository has a runnable backend package distinct from the bootstrap modules
    - Backend settings and auth configuration are typed and can be loaded from the existing config/secrets contract
    - Phase 2 test scaffolding exists before auth and persistence work expands
  artifacts:
    - pyproject.toml
    - src/transcendence_memory/backend/app.py
    - src/transcendence_memory/backend/settings.py
    - src/transcendence_memory/backend/auth/models.py
    - tests/conftest.py
  key_links:
    - pyproject.toml adds the runtime dependencies needed for FastAPI, Authlib, SQLAlchemy, Alembic, psycopg, pgvector, and HTTPX
    - backend settings consume the Phase 1 config/secrets storage pattern instead of inventing a second state path
---

<objective>
Create the Phase 2 backend foundation: dependencies, backend package scaffold, typed runtime settings, and shared auth models.

Purpose: All later Phase 2 work depends on a stable backend package boundary and typed auth/runtime config.
Output: FastAPI-ready backend scaffolding and Phase 2 dependency/test foundation.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authenticated-backend-core/02-CONTEXT.md
@.planning/phases/02-authenticated-backend-core/02-RESEARCH.md
@src/transcendence_memory/cli.py
@src/transcendence_memory/bootstrap/models.py
@src/transcendence_memory/bootstrap/persistence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 runtime dependencies and backend package scaffold</name>
  <files>pyproject.toml, src/transcendence_memory/backend/__init__.py, src/transcendence_memory/backend/app.py</files>
  <read_first>pyproject.toml, .planning/phases/02-authenticated-backend-core/02-RESEARCH.md, .planning/phases/02-authenticated-backend-core/02-VALIDATION.md</read_first>
  <action>Update `pyproject.toml` to add concrete Phase 2 runtime dependencies: `fastapi`, `uvicorn`, `sqlalchemy`, `alembic`, `psycopg`, `pgvector`, `httpx`, and `authlib`. Add the Phase 2 test/runtime support dependencies needed by the validation contract, such as `pytest-asyncio` if async tests are introduced. Create `src/transcendence_memory/backend/__init__.py` and `src/transcendence_memory/backend/app.py`. In `app.py`, define a FastAPI application factory or concrete app instance that is clearly separate from the Typer CLI and can later host `health`, `auth`, and `memory` routes.</action>
  <verify>rg -n "fastapi|uvicorn|sqlalchemy|alembic|psycopg|pgvector|authlib" pyproject.toml && test -f src/transcendence_memory/backend/app.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `fastapi`
    - `pyproject.toml` contains `authlib`
    - `pyproject.toml` contains `pgvector`
    - `src/transcendence_memory/backend/app.py` exists
  </acceptance_criteria>
  <done>The repository can host a backend runtime with explicit Phase 2 dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed backend settings and shared auth state models</name>
  <files>src/transcendence_memory/backend/settings.py, src/transcendence_memory/backend/auth/models.py, tests/conftest.py</files>
  <read_first>src/transcendence_memory/bootstrap/models.py, src/transcendence_memory/bootstrap/persistence.py, tests/conftest.py, .planning/phases/02-authenticated-backend-core/02-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/backend/settings.py` with concrete Pydantic settings/models for backend bind host, bind port, advertised URL, auth mode, OAuth issuer, OAuth client ID, OAuth scopes, provider name, model, and database URL. Extend the runtime model layer in `src/transcendence_memory/backend/auth/models.py` with concrete types for API-key metadata, OAuth token metadata, auth status output, and redacted token state. Update `tests/conftest.py` with fixtures for backend settings, temporary database URLs, and secret-path-backed auth state so later Phase 2 plans can test against the same contract.</action>
  <verify>rg -n "database_url|oauth|client_id|scopes|auth mode|api key" src/transcendence_memory/backend/settings.py src/transcendence_memory/backend/auth/models.py && rg -n "database|secret|settings" tests/conftest.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/backend/settings.py` contains `database_url`
    - `src/transcendence_memory/backend/settings.py` contains `client_id`
    - `src/transcendence_memory/backend/auth/models.py` contains `api_key`
    - `src/transcendence_memory/backend/auth/models.py` contains `token`
  </acceptance_criteria>
  <done>Backend runtime and auth configuration are typed and test fixtures can load them consistently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "fastapi|authlib|pgvector" pyproject.toml` succeeds
- [ ] `test -f src/transcendence_memory/backend/app.py` succeeds
- [ ] `rg -n "database_url|client_id" src/transcendence_memory/backend/settings.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Backend scaffolding is separate from bootstrap code
- Typed runtime/auth settings exist for later persistence and route work
</success_criteria>

<output>
After completion, create `.planning/phases/02-authenticated-backend-core/02-01-SUMMARY.md`
</output>
