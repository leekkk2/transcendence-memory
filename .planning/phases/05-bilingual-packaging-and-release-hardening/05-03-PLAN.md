---
phase: 05-bilingual-packaging-and-release-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - compat/release-compatibility.json
  - docs/release-compatibility.md
  - tests/unit/test_release_compatibility.py
autonomous: true
requirements:
  - VERI-05
user_setup: []
must_haves:
  truths:
    - The repository defines a machine-readable compatibility contract across skill, CLI, backend, and bundle versions
    - Compatibility rules are documented for human operators
    - Automated tests can reject missing or malformed compatibility data
  artifacts:
    - compat/release-compatibility.json
    - docs/release-compatibility.md
    - tests/unit/test_release_compatibility.py
  key_links:
    - compatibility manifest covers the actual in-repo skill package and bundle version
    - tests validate the manifest shape used by release workflows
---

<objective>
Add the release compatibility manifest and its validation layer.

Purpose: Phase 5 must turn compatibility from prose into an enforceable contract.
Output: Compatibility manifest, release compatibility doc, and tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@transcendence-memory/SKILL.md
@src/transcendence_memory/__init__.py
@src/transcendence_memory/handoff/models.py
@.planning/phases/05-bilingual-packaging-and-release-hardening/05-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create a machine-readable compatibility manifest and operator-facing compatibility doc</name>
  <files>compat/release-compatibility.json, docs/release-compatibility.md</files>
  <read_first>src/transcendence_memory/__init__.py, src/transcendence_memory/handoff/models.py, transcendence-memory/SKILL.md, .planning/phases/05-bilingual-packaging-and-release-hardening/05-RESEARCH.md</read_first>
  <action>Create `compat/release-compatibility.json` with explicit fields covering CLI version, backend version, skill version, bundle version, and compatible combinations. Use the current skill package version `0.1.0` and the current bundle version from the handoff model. Create `docs/release-compatibility.md` explaining the matrix in Chinese-first bilingual form and how operators should interpret compatibility failures.</action>
  <verify>rg -n "cli_version|backend_version|skill_version|bundle_version" compat/release-compatibility.json docs/release-compatibility.md</verify>
  <acceptance_criteria>
    - `compat/release-compatibility.json` contains `skill_version`
    - `compat/release-compatibility.json` contains `bundle_version`
    - `docs/release-compatibility.md` contains `bundle_version`
  </acceptance_criteria>
  <done>The repository has a real compatibility matrix rather than undocumented assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Add compatibility validation tests</name>
  <files>tests/unit/test_release_compatibility.py, compat/release-compatibility.json</files>
  <read_first>tests/unit/test_release_compatibility.py, compat/release-compatibility.json, .planning/phases/05-bilingual-packaging-and-release-hardening/05-VALIDATION.md</read_first>
  <action>Create `tests/unit/test_release_compatibility.py` with concrete checks that the compatibility manifest exists, parses as JSON, and contains non-empty values for CLI, backend, skill, and bundle versions. Add checks that the current bundle version from code and the documented skill version align with the manifest.</action>
  <verify>test -f tests/unit/test_release_compatibility.py && rg -n "skill_version|bundle_version|backend_version|cli_version" tests/unit/test_release_compatibility.py</verify>
  <acceptance_criteria>
    - `tests/unit/test_release_compatibility.py` exists
    - `tests/unit/test_release_compatibility.py` contains `skill_version`
    - `tests/unit/test_release_compatibility.py` contains `bundle_version`
  </acceptance_criteria>
  <done>Compatibility rules are validated automatically.</done>
</task>
</tasks>

<verification>
- [ ] `rg -n "cli_version|backend_version|skill_version|bundle_version" compat/release-compatibility.json` succeeds
- [ ] `test -f tests/unit/test_release_compatibility.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Compatibility is a checked artifact, not just documentation
</success_criteria>

<output>
After completion, create `.planning/phases/05-bilingual-packaging-and-release-hardening/05-03-SUMMARY.md`
</output>
