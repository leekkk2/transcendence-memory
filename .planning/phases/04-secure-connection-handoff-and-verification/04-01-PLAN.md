---
phase: 04-secure-connection-handoff-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/transcendence_memory/handoff/models.py
  - src/transcendence_memory/handoff/sanitize.py
  - tests/unit/test_connection_bundle.py
  - tests/unit/test_endpoint_sanitization.py
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-05
user_setup: []
must_haves:
  truths:
    - The connection bundle is a typed, versioned contract rather than an ad hoc JSON blob
    - Split-machine handoff rejects local-only endpoints and other unsafe export targets
    - Bundle payloads never contain secret auth material
  artifacts:
    - src/transcendence_memory/handoff/models.py
    - src/transcendence_memory/handoff/sanitize.py
    - tests/unit/test_connection_bundle.py
    - tests/unit/test_endpoint_sanitization.py
  key_links:
    - bundle model explicitly separates safe metadata from secret state
    - sanitization logic is reusable by both backend export and frontend import code
---

<objective>
Create the shared connection bundle contract and safety rules.

Purpose: Phase 4 cannot be safe without a typed bundle schema and explicit endpoint sanitization.
Output: Bundle models, sanitization helpers, and unit coverage for redaction and public-endpoint rules.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-secure-connection-handoff-and-verification/04-CONTEXT.md
@.planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md
@src/transcendence_memory/bootstrap/models.py
@src/transcendence_memory/backend/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed connection bundle and compatibility models</name>
  <files>src/transcendence_memory/handoff/models.py, tests/unit/test_connection_bundle.py</files>
  <read_first>src/transcendence_memory/bootstrap/models.py, src/transcendence_memory/backend/settings.py, .planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/handoff/models.py` with typed Pydantic models for a versioned connection bundle. Include concrete sections for `bundle_version`, `generated_at`, `topology`, `backend` advertised URL and route paths, `auth` mode metadata and required local inputs, `provider` metadata, and compatibility/version fields. Do not include API keys, access tokens, refresh tokens, or secret file contents in any serializable bundle model. Add `tests/unit/test_connection_bundle.py` that asserts those fields exist and that secret-bearing keys are absent from the serialized payload.</action>
  <verify>rg -n "bundle_version|generated_at|advertised_url|required_local_inputs|compatibility" src/transcendence_memory/handoff/models.py tests/unit/test_connection_bundle.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/handoff/models.py` contains `bundle_version`
    - `src/transcendence_memory/handoff/models.py` contains `advertised_url`
    - `tests/unit/test_connection_bundle.py` contains `required_local_inputs`
    - `tests/unit/test_connection_bundle.py` verifies secret-bearing keys are absent
  </acceptance_criteria>
  <done>The project has a typed, versioned bundle contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add endpoint sanitization rules for same-machine and split-machine handoff</name>
  <files>src/transcendence_memory/handoff/sanitize.py, tests/unit/test_endpoint_sanitization.py</files>
  <read_first>src/transcendence_memory/backend/settings.py, .planning/phases/04-secure-connection-handoff-and-verification/04-CONTEXT.md, .planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/handoff/sanitize.py` with concrete helpers that classify `127.0.0.1`, `localhost`, and container-local hostnames as local-only endpoints. Allow them for same-machine bundles only. Reject or flag them for split-machine bundles unless an explicitly advertised public URL is configured. Add `tests/unit/test_endpoint_sanitization.py` with same-machine allow cases and split-machine reject cases.</action>
  <verify>rg -n "127.0.0.1|localhost|same_machine|split_machine|advertised" src/transcendence_memory/handoff/sanitize.py tests/unit/test_endpoint_sanitization.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/handoff/sanitize.py` contains `127.0.0.1`
    - `src/transcendence_memory/handoff/sanitize.py` contains `localhost`
    - `tests/unit/test_endpoint_sanitization.py` contains `same_machine`
    - `tests/unit/test_endpoint_sanitization.py` contains `split_machine`
  </acceptance_criteria>
  <done>Endpoint safety rules are explicit and test-covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "bundle_version|advertised_url" src/transcendence_memory/handoff/models.py` succeeds
- [ ] `rg -n "127.0.0.1|localhost|split_machine" src/transcendence_memory/handoff/sanitize.py` succeeds
- [ ] `test -f tests/unit/test_connection_bundle.py && test -f tests/unit/test_endpoint_sanitization.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Bundle contract is typed and versioned
- Split-machine export safety is enforced in code
</success_criteria>

<output>
After completion, create `.planning/phases/04-secure-connection-handoff-and-verification/04-01-SUMMARY.md`
</output>
