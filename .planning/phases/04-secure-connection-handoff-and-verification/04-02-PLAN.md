---
phase: 04-secure-connection-handoff-and-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/transcendence_memory/cli.py
  - src/transcendence_memory/handoff/export.py
  - tests/cli/test_backend_export_connection.py
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-05
user_setup: []
must_haves:
  truths:
    - Backend users can export a versioned redacted connection bundle
    - The export path includes only safe metadata and no secret auth material
    - Split-machine exports refuse local-only backend endpoints
  artifacts:
    - src/transcendence_memory/handoff/export.py
    - src/transcendence_memory/cli.py
    - tests/cli/test_backend_export_connection.py
  key_links:
    - backend export reads runtime metadata from current config/settings rather than inventing a second source of truth
    - export command uses the shared sanitization and bundle models from Plan 04-01
---

<objective>
Add the backend export path for redacted connection bundles.

Purpose: Split-machine handoff begins on the backend host, and that flow must be safe before any frontend import exists.
Output: Bundle export helper, CLI `backend export-connection`, and export-path tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-secure-connection-handoff-and-verification/04-CONTEXT.md
@.planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md
@src/transcendence_memory/cli.py
@src/transcendence_memory/backend/settings.py
@src/transcendence_memory/handoff/models.py
@src/transcendence_memory/handoff/sanitize.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement backend bundle export helpers</name>
  <files>src/transcendence_memory/handoff/export.py, tests/cli/test_backend_export_connection.py</files>
  <read_first>src/transcendence_memory/backend/settings.py, src/transcendence_memory/handoff/models.py, src/transcendence_memory/handoff/sanitize.py, .planning/phases/04-secure-connection-handoff-and-verification/04-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/handoff/export.py` with helpers that build a versioned redacted connection bundle from current runtime settings, auth mode metadata, and memory route paths. Make the export path include only non-secret metadata and explicitly list any `required_local_inputs` the receiving machine must resolve. Add `tests/cli/test_backend_export_connection.py` that asserts API keys and token values are absent from exported output and that split-machine export rejects local-only advertised URLs.</action>
  <verify>rg -n "required_local_inputs|bundle_version|advertised_url|auth_mode" src/transcendence_memory/handoff/export.py tests/cli/test_backend_export_connection.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/handoff/export.py` contains `required_local_inputs`
    - `src/transcendence_memory/handoff/export.py` contains `auth_mode`
    - `tests/cli/test_backend_export_connection.py` asserts secret values are absent
    - `tests/cli/test_backend_export_connection.py` covers split-machine rejection
  </acceptance_criteria>
  <done>The backend can construct a safe redacted bundle from existing runtime state.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI backend export command</name>
  <files>src/transcendence_memory/cli.py, tests/cli/test_backend_export_connection.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/handoff/export.py, .planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md</read_first>
  <action>Extend `src/transcendence_memory/cli.py` with `backend export-connection`. Make it accept an explicit topology or export mode, serialize the redacted bundle, and print or write the bundle in a copy/paste-friendly format. If the backend is configured only with local-only endpoints for a split-machine export, fail with actionable guidance instead of exporting unsafe data.</action>
  <verify>rg -n "backend export-connection|split-machine|required_local_inputs|advertised_url" src/transcendence_memory/cli.py tests/cli/test_backend_export_connection.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `backend export-connection`
    - `src/transcendence_memory/cli.py` contains split-machine safety guidance
    - `tests/cli/test_backend_export_connection.py` covers CLI export output
  </acceptance_criteria>
  <done>The operator has a first-class backend export command for handoff bundles.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "bundle_version|required_local_inputs" src/transcendence_memory/handoff/export.py` succeeds
- [ ] `rg -n "backend export-connection" src/transcendence_memory/cli.py` succeeds
- [ ] `test -f tests/cli/test_backend_export_connection.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Backend export is safe, redacted, and split-machine aware
</success_criteria>

<output>
After completion, create `.planning/phases/04-secure-connection-handoff-and-verification/04-02-SUMMARY.md`
</output>
