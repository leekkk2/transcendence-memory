---
phase: 04-secure-connection-handoff-and-verification
plan: 04
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - src/transcendence_memory/cli.py
  - src/transcendence_memory/handoff/smoke.py
  - tests/cli/test_smoke_command.py
  - tests/integration/test_same_machine_smoke.py
  - tests/integration/test_split_machine_bundle_flow.py
autonomous: true
requirements:
  - VERI-01
  - CONN-04
user_setup: []
must_haves:
  truths:
    - Users can run an end-to-end smoke flow covering `health`, `embed`, and `search`
    - Smoke verification works against the imported connection metadata and local auth state
    - Same-machine and split-machine paths both have verification coverage, with environment-gated integration where necessary
  artifacts:
    - src/transcendence_memory/handoff/smoke.py
    - src/transcendence_memory/cli.py
    - tests/cli/test_smoke_command.py
    - tests/integration/test_same_machine_smoke.py
    - tests/integration/test_split_machine_bundle_flow.py
  key_links:
    - smoke verification calls the existing backend health and memory endpoints instead of inventing a new API
    - CLI smoke behavior follows the same imported bundle/auth contract as frontend check
---

<objective>
Add the end-to-end smoke verification surface for same-machine and split-machine flows.

Purpose: Phase 4 is only complete if operators can prove the handoff path works end to end.
Output: Smoke helper module, CLI smoke command, and verification tests.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-secure-connection-handoff-and-verification/04-CONTEXT.md
@.planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md
@src/transcendence_memory/deploy/health.py
@src/transcendence_memory/backend/api/routes/memory.py
@src/transcendence_memory/cli.py
@src/transcendence_memory/handoff/importer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement smoke verification helpers for health, embed, and search</name>
  <files>src/transcendence_memory/handoff/smoke.py, tests/cli/test_smoke_command.py</files>
  <read_first>src/transcendence_memory/deploy/health.py, src/transcendence_memory/backend/api/routes/memory.py, .planning/phases/04-secure-connection-handoff-and-verification/04-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/handoff/smoke.py` with helpers that call the existing backend health route and then execute authenticated `embed` and `search` requests against the current imported connection profile. Add `tests/cli/test_smoke_command.py` that verifies the smoke sequence order and asserts the command reports health/embed/search stages clearly.</action>
  <verify>rg -n "health|embed|search|smoke" src/transcendence_memory/handoff/smoke.py tests/cli/test_smoke_command.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/handoff/smoke.py` contains `health`
    - `src/transcendence_memory/handoff/smoke.py` contains `embed`
    - `src/transcendence_memory/handoff/smoke.py` contains `search`
    - `tests/cli/test_smoke_command.py` contains all three stage names
  </acceptance_criteria>
  <done>Smoke verification logic exists as a reusable helper, not a CLI-only inline script.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI smoke command and same-machine/split-machine integration coverage</name>
  <files>src/transcendence_memory/cli.py, tests/integration/test_same_machine_smoke.py, tests/integration/test_split_machine_bundle_flow.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/handoff/smoke.py, src/transcendence_memory/handoff/export.py, src/transcendence_memory/handoff/importer.py, .planning/phases/04-secure-connection-handoff-and-verification/04-CONTEXT.md</read_first>
  <action>Extend `src/transcendence_memory/cli.py` with a smoke-verification command for the imported frontend connection context. Add `tests/integration/test_same_machine_smoke.py` and `tests/integration/test_split_machine_bundle_flow.py` as environment-gated integration coverage for same-machine and split-machine verification paths. These tests should validate flow shape and bundle/import/smoke interaction without forcing live infrastructure in all environments.</action>
  <verify>rg -n "smoke" src/transcendence_memory/cli.py tests/integration/test_same_machine_smoke.py tests/integration/test_split_machine_bundle_flow.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains a smoke command
    - `tests/integration/test_same_machine_smoke.py` exists
    - `tests/integration/test_split_machine_bundle_flow.py` exists
  </acceptance_criteria>
  <done>Operators have a CLI-level end-to-end verification path for imported connections.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "health|embed|search" src/transcendence_memory/handoff/smoke.py` succeeds
- [ ] `rg -n "smoke" src/transcendence_memory/cli.py` succeeds
- [ ] `test -f tests/cli/test_smoke_command.py && test -f tests/integration/test_same_machine_smoke.py && test -f tests/integration/test_split_machine_bundle_flow.py` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Same-machine and split-machine smoke verification surfaces exist
</success_criteria>

<output>
After completion, create `.planning/phases/04-secure-connection-handoff-and-verification/04-04-SUMMARY.md`
</output>
