---
phase: 01-guided-bootstrap-and-safe-configuration
plan: 05
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - transcendence-memory/SKILL.md
  - transcendence-memory/references/bootstrap.md
  - transcendence-memory/references/troubleshooting.md
  - transcendence-memory/assets/README.txt
autonomous: true
requirements:
  - BOOT-01
  - BOOT-05
user_setup: []
must_haves:
  truths:
    - The OpenClaw skill acts as the guidance surface and points operators to the canonical CLI bootstrap flow
    - Same-machine and split-machine guidance is available without requiring domain or reverse-proxy inputs up front
    - The skill docs preserve the non-blocking `IP + port` fallback and do not collapse into backend runtime implementation
  artifacts:
    - transcendence-memory/SKILL.md
    - transcendence-memory/references/bootstrap.md
    - transcendence-memory/references/troubleshooting.md
  key_links:
    - the skill references the canonical CLI commands introduced in Plan 04
    - troubleshooting guidance matches doctor classifications and rerun behavior from Plan 04
---

<objective>
Create the Phase 1 OpenClaw skill guidance surface and bilingual bootstrap references that point to the canonical CLI behavior.

Purpose: The user explicitly wants the skill to be the primary AI-assisted entrypoint, but it must stay thin and reference the CLI instead of becoming deployment logic.
Output: A lean skill package with bootstrap and troubleshooting references for same-machine and split-machine Phase 1 flows.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md
@src/transcendence_memory/cli.py
@src/transcendence_memory/bootstrap/doctor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a thin OpenClaw skill package that guides operators into the CLI bootstrap flow</name>
  <files>transcendence-memory/SKILL.md, transcendence-memory/assets/README.txt</files>
  <read_first>transcendence-memory/SKILL.md, src/transcendence_memory/cli.py, .planning/PROJECT.md, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md</read_first>
  <action>Create `transcendence-memory/SKILL.md` using the `skill-name/` package layout the user requested: YAML frontmatter with `name` and `description`, a lean body that explains the skill’s role as the primary guidance surface, and direct references to canonical commands `transcendence-memory init backend`, `transcendence-memory init frontend`, `transcendence-memory init both`, `transcendence-memory config show`, and `transcendence-memory doctor`. Make it explicit that the skill guides and invokes the CLI rather than embedding backend runtime logic. Add `transcendence-memory/assets/README.txt` as a placeholder describing the assets directory contract so later phases have a stable package layout.</action>
  <verify>rg -n "name:|description:|transcendence-memory init backend|doctor" transcendence-memory/SKILL.md && test -f transcendence-memory/assets/README.txt</verify>
  <acceptance_criteria>
    - `transcendence-memory/SKILL.md` contains `name:`
    - `transcendence-memory/SKILL.md` contains `description:`
    - `transcendence-memory/SKILL.md` contains `transcendence-memory init backend`
    - `transcendence-memory/SKILL.md` contains `transcendence-memory doctor`
  </acceptance_criteria>
  <done>The skill package exists as a thin guidance layer with the correct command surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add bootstrap and troubleshooting references for same-machine and split-machine flows</name>
  <files>transcendence-memory/references/bootstrap.md, transcendence-memory/references/troubleshooting.md</files>
  <read_first>transcendence-memory/references/bootstrap.md, transcendence-memory/references/troubleshooting.md, src/transcendence_memory/cli.py, src/transcendence_memory/bootstrap/doctor.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md</read_first>
  <action>Create `transcendence-memory/references/bootstrap.md` and `transcendence-memory/references/troubleshooting.md` with Chinese-first bilingual content. The bootstrap reference must document same-machine and split-machine entry flows, must recommend `both + same_machine` for novices, and must explicitly describe the `IP + port` fallback when domain or reverse-proxy data is absent. The troubleshooting reference must mirror `doctor` classifications `auto-fixable`, `needs input`, and `manual follow-up`, and it must point users to rerun/bootstrap-safe commands rather than backend runtime commands.</action>
  <verify>rg -n "same-machine|split-machine|IP \\+ port|auto-fixable|needs input|manual follow-up" transcendence-memory/references/bootstrap.md transcendence-memory/references/troubleshooting.md</verify>
  <acceptance_criteria>
    - `transcendence-memory/references/bootstrap.md` contains `same-machine`
    - `transcendence-memory/references/bootstrap.md` contains `split-machine`
    - `transcendence-memory/references/bootstrap.md` contains `IP + port`
    - `transcendence-memory/references/troubleshooting.md` contains `auto-fixable`
  </acceptance_criteria>
  <done>Skill references preserve the agreed Phase 1 operator behavior and fallback guidance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "transcendence-memory init backend|transcendence-memory doctor" transcendence-memory/SKILL.md` succeeds
- [ ] `rg -n "same-machine|split-machine|IP \\+ port" transcendence-memory/references/bootstrap.md` succeeds
- [ ] `rg -n "auto-fixable|needs input|manual follow-up" transcendence-memory/references/troubleshooting.md` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The skill package stays thin and CLI-referential
- Same-machine, split-machine, and `IP + port` fallback guidance are all documented in the skill references
</success_criteria>

<output>
After completion, create `.planning/phases/01-guided-bootstrap-and-safe-configuration/01-05-SUMMARY.md`
</output>
