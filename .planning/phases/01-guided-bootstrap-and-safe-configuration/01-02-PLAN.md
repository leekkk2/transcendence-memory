---
phase: 01-guided-bootstrap-and-safe-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/transcendence_memory/bootstrap/paths.py
  - src/transcendence_memory/bootstrap/persistence.py
  - tests/unit/test_platform_paths.py
  - tests/unit/test_config_persistence.py
autonomous: true
requirements:
  - CONF-02
  - CONF-03
  - CONF-05
user_setup: []
must_haves:
  truths:
    - Non-secret config and secret storage use separate platform-native roots
    - Config and secret paths can be overridden explicitly from the CLI surface
    - Bootstrap persistence stores enough structured state for safe rerun later
  artifacts:
    - src/transcendence_memory/bootstrap/paths.py
    - src/transcendence_memory/bootstrap/persistence.py
    - tests/unit/test_platform_paths.py
    - tests/unit/test_config_persistence.py
  key_links:
    - path helpers use platform conventions and feed persistence logic
    - persistence code writes non-secret config and secret files to different locations
---

<objective>
Implement platform-aware path resolution and bootstrap persistence for non-secret config versus secret state.

Purpose: Phase 1 must lock down where local state lives before detection, rerun, and doctor logic can be trusted.
Output: Path helpers, config/secret persistence helpers, and unit tests proving separation and override behavior.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md
@src/transcendence_memory/bootstrap/models.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement platform-native config and secret path resolution</name>
  <files>src/transcendence_memory/bootstrap/paths.py, tests/unit/test_platform_paths.py</files>
  <read_first>src/transcendence_memory/bootstrap/paths.py, src/transcendence_memory/bootstrap/models.py, tests/conftest.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/bootstrap/paths.py` with functions that resolve config root, secret root, and profile paths using `platformdirs`. Encode the concrete platform rules from research: Linux uses XDG-style config paths, macOS uses `~/Library/Application Support/transcendence-memory/`, and Windows uses `%APPDATA%\\transcendence-memory\\`. Add explicit support for CLI override inputs such as `--config-path` and `--secret-path`. Create `tests/unit/test_platform_paths.py` that simulates Linux, macOS, and Windows environments and verifies the returned paths contain the expected platform-specific root segments.</action>
  <verify>rg -n "platformdirs|config|secret|Application Support|APPDATA" src/transcendence_memory/bootstrap/paths.py && test -f tests/unit/test_platform_paths.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/bootstrap/paths.py` contains `platformdirs`
    - `src/transcendence_memory/bootstrap/paths.py` contains `Application Support`
    - `tests/unit/test_platform_paths.py` contains `linux`
    - `tests/unit/test_platform_paths.py` contains `windows`
  </acceptance_criteria>
  <done>Path helpers resolve config and secret roots consistently across supported platforms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement separate persistence for config state and secret state</name>
  <files>src/transcendence_memory/bootstrap/persistence.py, tests/unit/test_config_persistence.py</files>
  <read_first>src/transcendence_memory/bootstrap/persistence.py, src/transcendence_memory/bootstrap/models.py, src/transcendence_memory/bootstrap/paths.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/bootstrap/persistence.py` with concrete helpers to write and read a human-readable non-secret config file plus a separate secret file. Persist role, topology, provider, model, base URL, selected transport hint, and schema version in the non-secret file. Persist API keys or other secret placeholders in the secret file only. Add permission enforcement helpers for secret storage and create `tests/unit/test_config_persistence.py` that proves secret values never land in the non-secret config file, proves override paths are honored, and proves missing directories are created safely.</action>
  <verify>rg -n "schema_version|base_url|provider|secret" src/transcendence_memory/bootstrap/persistence.py && test -f tests/unit/test_config_persistence.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/bootstrap/persistence.py` contains `schema_version`
    - `src/transcendence_memory/bootstrap/persistence.py` contains `base_url`
    - `tests/unit/test_config_persistence.py` contains `secret`
    - `tests/unit/test_config_persistence.py` contains `config`
  </acceptance_criteria>
  <done>Non-secret config and secret state are persisted separately and test-covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "platformdirs" src/transcendence_memory/bootstrap/paths.py` succeeds
- [ ] `rg -n "schema_version|base_url|provider" src/transcendence_memory/bootstrap/persistence.py` succeeds
- [ ] `test -f tests/unit/test_platform_paths.py && test -f tests/unit/test_config_persistence.py` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Config and secret storage are separate by implementation, not just by documentation
- Override path support exists for later CLI wiring
</success_criteria>

<output>
After completion, create `.planning/phases/01-guided-bootstrap-and-safe-configuration/01-02-SUMMARY.md`
</output>
