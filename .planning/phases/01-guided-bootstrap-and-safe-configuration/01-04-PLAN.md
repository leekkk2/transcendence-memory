---
phase: 01-guided-bootstrap-and-safe-configuration
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - src/transcendence_memory/cli.py
  - src/transcendence_memory/bootstrap/doctor.py
  - tests/cli/test_init_command.py
  - tests/cli/test_doctor_command.py
  - tests/integration/test_bootstrap_rerun.py
autonomous: true
requirements:
  - BOOT-04
  - CONF-01
  - CONF-04
  - CONF-05
user_setup: []
must_haves:
  truths:
    - Users can run canonical `init` and `doctor` commands against the structured bootstrap state
    - Rerun uses a diff-style plan rather than silent overwrite behavior
    - Users can inspect non-secret config safely and pass explicit config or secret path overrides
  artifacts:
    - src/transcendence_memory/cli.py
    - src/transcendence_memory/bootstrap/doctor.py
    - tests/cli/test_init_command.py
    - tests/cli/test_doctor_command.py
    - tests/integration/test_bootstrap_rerun.py
  key_links:
    - CLI commands call the path, persistence, detection, and plan builder layers rather than reimplementing logic inline
    - doctor classifications map to safe auto-fix behavior and next-step guidance
---

<objective>
Implement the Phase 1 CLI behavior for init, safe config inspection, rerun diffing, and bootstrap-scoped doctor diagnostics.

Purpose: This is the operator surface that turns the Phase 1 contracts into usable commands.
Output: Working CLI flows for `init`, safe config inspection, path overrides, rerun diff behavior, and `doctor`.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md
@src/transcendence_memory/bootstrap/models.py
@src/transcendence_memory/bootstrap/paths.py
@src/transcendence_memory/bootstrap/persistence.py
@src/transcendence_memory/bootstrap/detect.py
@src/transcendence_memory/bootstrap/planner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical init commands with provider settings and path override support</name>
  <files>src/transcendence_memory/cli.py, tests/cli/test_init_command.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/bootstrap/models.py, src/transcendence_memory/bootstrap/persistence.py, src/transcendence_memory/bootstrap/planner.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md</read_first>
  <action>Extend `src/transcendence_memory/cli.py` so it supports canonical commands `transcendence-memory init backend`, `transcendence-memory init frontend`, and `transcendence-memory init both`. Add concrete flags `--topology same_machine|split_machine`, `--provider`, `--model`, `--base-url`, `--config-path`, `--secret-path`, and `--dry-run`. If `--topology` is omitted, the CLI should show the detected recommendation and allow the operator to confirm or override it before mutation. Wire these commands to the typed models, detection layer, persistence layer, and plan builder instead of duplicating logic. Add `tests/cli/test_init_command.py` covering dry-run output, role selection, topology selection for `same_machine` and `split_machine`, override path handling, and persistence of provider/model/base URL in the non-secret config file.</action>
  <verify>rg -n "init backend|init frontend|init both|topology|same_machine|split_machine|base-url|config-path|secret-path|dry-run" src/transcendence_memory/cli.py && test -f tests/cli/test_init_command.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `init backend`
    - `src/transcendence_memory/cli.py` contains `init frontend`
    - `src/transcendence_memory/cli.py` contains `same_machine`
    - `src/transcendence_memory/cli.py` contains `split_machine`
    - `src/transcendence_memory/cli.py` contains `--base-url`
    - `tests/cli/test_init_command.py` contains `same_machine`
    - `tests/cli/test_init_command.py` contains `split_machine`
  </acceptance_criteria>
  <done>The canonical init surface exists and is wired to the Phase 1 layers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement safe config inspection, rerun diff behavior, and bootstrap doctor</name>
  <files>src/transcendence_memory/cli.py, src/transcendence_memory/bootstrap/doctor.py, tests/cli/test_doctor_command.py, tests/integration/test_bootstrap_rerun.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/bootstrap/doctor.py, src/transcendence_memory/bootstrap/persistence.py, src/transcendence_memory/bootstrap/detect.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md</read_first>
  <action>Add a safe config inspection command such as `transcendence-memory config show` that prints non-secret config only and never prints secret values. Implement rerun behavior so repeated `init` calls compute and display a diff-style plan before overwriting non-secret configuration. Create `src/transcendence_memory/bootstrap/doctor.py` with bootstrap-scoped checks for config writability, secret path existence, Docker availability, config schema validity, missing files, and obvious port conflicts. Support `doctor --fix` only for safe local repairs such as directory creation, non-secret config regeneration, and permission repair. Add CLI tests in `tests/cli/test_doctor_command.py` and integration coverage in `tests/integration/test_bootstrap_rerun.py` for rerun safety and doctor classification outputs `auto-fixable`, `needs input`, and `manual follow-up`.</action>
  <verify>rg -n "config show|doctor|auto-fixable|needs input|manual follow-up" src/transcendence_memory/cli.py src/transcendence_memory/bootstrap/doctor.py && test -f tests/cli/test_doctor_command.py && test -f tests/integration/test_bootstrap_rerun.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `config show`
    - `src/transcendence_memory/bootstrap/doctor.py` contains `auto-fixable`
    - `src/transcendence_memory/bootstrap/doctor.py` contains `needs input`
    - `tests/integration/test_bootstrap_rerun.py` contains `rerun`
  </acceptance_criteria>
  <done>Rerun and doctor behavior are concrete, safe, and test-covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "init backend|init frontend|config show" src/transcendence_memory/cli.py` succeeds
- [ ] `rg -n "auto-fixable|needs input|manual follow-up" src/transcendence_memory/bootstrap/doctor.py` succeeds
- [ ] `test -f tests/cli/test_init_command.py && test -f tests/cli/test_doctor_command.py && test -f tests/integration/test_bootstrap_rerun.py` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- CLI init/rerun/inspect/doctor behavior is safe and non-secret by default
- Path overrides and provider settings are supported without editing repository files
</success_criteria>

<output>
After completion, create `.planning/phases/01-guided-bootstrap-and-safe-configuration/01-04-SUMMARY.md`
</output>
