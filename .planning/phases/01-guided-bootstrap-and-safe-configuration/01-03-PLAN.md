---
phase: 01-guided-bootstrap-and-safe-configuration
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/transcendence_memory/bootstrap/detect.py
  - src/transcendence_memory/bootstrap/planner.py
  - tests/unit/test_plan_builder.py
autonomous: true
requirements:
  - BOOT-02
  - BOOT-03
user_setup: []
must_haves:
  truths:
    - Bootstrap can inspect the current machine and generate a structured dry-run plan before any mutation
    - Role and topology recommendations are explainable rather than opaque
    - Incomplete networking information falls back to `ip_port` without blocking the phase
  artifacts:
    - src/transcendence_memory/bootstrap/detect.py
    - src/transcendence_memory/bootstrap/planner.py
    - tests/unit/test_plan_builder.py
  key_links:
    - detection output feeds the dry-run plan builder
    - the plan builder records deferred domain/proxy work when richer inputs are absent
---

<objective>
Implement environment detection and dry-run plan generation for bootstrap.

Purpose: Users must see a real plan before state changes, and later CLI commands depend on machine inspection plus deterministic recommendations.
Output: Detection helpers, dry-run plan builder, and tests for plan rendering and fallback behavior.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md
@src/transcendence_memory/bootstrap/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement environment detection for bootstrap-relevant signals</name>
  <files>src/transcendence_memory/bootstrap/detect.py, tests/unit/test_plan_builder.py</files>
  <read_first>src/transcendence_memory/bootstrap/detect.py, src/transcendence_memory/bootstrap/models.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/bootstrap/detect.py` with concrete detection helpers for operating system, shell, Docker CLI availability, Docker Compose availability, writability checks for caller-provided candidate config and secret paths, and common default port conflicts. Return structured results rather than printing prose. Include recommendation helpers that can suggest `role=both` and `topology=same_machine` when no stronger signal exists. Extend `tests/unit/test_plan_builder.py` or adjacent fixtures so detection outputs can be used in deterministic plan-building tests.</action>
  <verify>rg -n "docker|compose|writable|port|same_machine|both" src/transcendence_memory/bootstrap/detect.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/bootstrap/detect.py` contains `docker`
    - `src/transcendence_memory/bootstrap/detect.py` contains `compose`
    - `src/transcendence_memory/bootstrap/detect.py` contains `same_machine`
    - `src/transcendence_memory/bootstrap/detect.py` contains `both`
  </acceptance_criteria>
  <done>Bootstrap-relevant machine inspection exists and can drive dry-run planning.</done>
</task>

<task type="auto">
  <name>Task 2: Implement a structured dry-run plan builder with deferred networking notes</name>
  <files>src/transcendence_memory/bootstrap/planner.py, tests/unit/test_plan_builder.py</files>
  <read_first>src/transcendence_memory/bootstrap/planner.py, src/transcendence_memory/bootstrap/detect.py, src/transcendence_memory/bootstrap/models.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/bootstrap/planner.py` with a plan model and renderer that groups plan output into findings, selected role/topology, file and directory mutations, secret path initialization, warnings, deferred items, and post-run verification commands. Encode the concrete fallback rule that missing domain or reverse-proxy input should produce `transport_hint=ip_port` and a deferred note for later upgrade instead of blocking. Add tests in `tests/unit/test_plan_builder.py` that assert dry-run plans contain explicit sections, recommendation reasons, and a deferred networking message when domain/proxy inputs are absent.</action>
  <verify>rg -n "deferred|ip_port|warnings|verification|dry" src/transcendence_memory/bootstrap/planner.py && test -f tests/unit/test_plan_builder.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/bootstrap/planner.py` contains `ip_port`
    - `src/transcendence_memory/bootstrap/planner.py` contains `deferred`
    - `tests/unit/test_plan_builder.py` contains `ip_port`
    - `tests/unit/test_plan_builder.py` contains `verification`
  </acceptance_criteria>
  <done>Dry-run planning is structured, testable, and aligned with the non-blocking fallback requirement.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "docker|compose|writable|port" src/transcendence_memory/bootstrap/detect.py` succeeds
- [ ] `rg -n "ip_port|deferred|verification" src/transcendence_memory/bootstrap/planner.py` succeeds
- [ ] `test -f tests/unit/test_plan_builder.py` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dry-run planning exists as a typed and testable object, not just CLI prose
- Missing networking inputs trigger documented fallback instead of bootstrap failure
</success_criteria>

<output>
After completion, create `.planning/phases/01-guided-bootstrap-and-safe-configuration/01-03-SUMMARY.md`
</output>
