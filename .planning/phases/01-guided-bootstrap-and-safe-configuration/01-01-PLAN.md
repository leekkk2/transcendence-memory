---
phase: 01-guided-bootstrap-and-safe-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/transcendence_memory/__init__.py
  - src/transcendence_memory/cli.py
  - src/transcendence_memory/bootstrap/models.py
  - tests/conftest.py
autonomous: true
requirements:
  - BOOT-01
  - CONF-02
user_setup: []
must_haves:
  truths:
    - The repository exposes a canonical Python package and CLI entry point for `transcendence-memory`
    - Role and topology are represented by typed bootstrap models rather than ad hoc strings
    - Test infrastructure exists before later plans add behavior-heavy code
  artifacts:
    - pyproject.toml
    - src/transcendence_memory/cli.py
    - src/transcendence_memory/bootstrap/models.py
    - tests/conftest.py
  key_links:
    - pyproject.toml defines the `transcendence-memory` console entry point that imports `transcendence_memory.cli:app`
    - bootstrap models define role/topology structures consumed by later config, plan, and doctor code
---

<objective>
Establish the Phase 1 execution foundation: Python project wiring, CLI entry surface, typed bootstrap models, and baseline test scaffolding.

Purpose: Every later Phase 1 plan depends on a stable project layout and typed contracts.
Output: Executable project skeleton with a callable CLI entry point, bootstrap enums/models, and pytest scaffolding.
</objective>

<execution_context>
@/Users/zykj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zykj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md
@.planning/phases/01-guided-bootstrap-and-safe-configuration/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Python package, uv project wiring, and test harness</name>
  <files>pyproject.toml, src/transcendence_memory/__init__.py, tests/conftest.py</files>
  <read_first>pyproject.toml, .planning/PROJECT.md, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-VALIDATION.md</read_first>
  <action>Create `pyproject.toml` for a `uv`-managed Python 3.13 project named `transcendence-memory`. Define a console script entry point `transcendence-memory = transcendence_memory.cli:app`. Add runtime dependencies needed for Phase 1 scaffolding only: `typer`, `pydantic`, `pydantic-settings`, `platformdirs`, and `rich` if used for plan rendering. Add dev/test dependencies needed by the validation contract: `pytest`. Create `src/transcendence_memory/__init__.py` with a package version placeholder and create `tests/conftest.py` with fixtures for temporary config roots, temporary secret roots, and environment monkeypatch helpers. Do not add backend, database, or OAuth runtime dependencies in this plan.</action>
  <verify>rg -n "transcendence_memory\\.cli:app|platformdirs|pytest" pyproject.toml && test -f src/transcendence_memory/__init__.py && test -f tests/conftest.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `transcendence-memory = "transcendence_memory.cli:app"` or equivalent console script wiring
    - `pyproject.toml` contains `platformdirs`
    - `pyproject.toml` contains `pytest`
    - `tests/conftest.py` exists
  </acceptance_criteria>
  <done>Project packaging and test harness exist and are ready for later Phase 1 code.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed bootstrap models for role, topology, transport, and settings versioning</name>
  <files>src/transcendence_memory/bootstrap/models.py, src/transcendence_memory/__init__.py</files>
  <read_first>src/transcendence_memory/bootstrap/models.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-RESEARCH.md</read_first>
  <action>Create `src/transcendence_memory/bootstrap/models.py` and define explicit enums and Pydantic models for `Role`, `Topology`, `TransportHint`, `BootstrapMode`, `ProviderSettings`, `BootstrapSelection`, and `BootstrapState`. Include concrete enum values `backend`, `frontend`, `both`, `same_machine`, `split_machine`, `ip_port`, and `domain_proxy`. Include a schema version field in `BootstrapState` so rerun and doctor flows can detect drift later. Export the core model names from the package where appropriate so later plans can import them without redefining strings.</action>
  <verify>rg -n "class Role|backend|frontend|both|same_machine|split_machine|schema_version" src/transcendence_memory/bootstrap/models.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/bootstrap/models.py` contains `class Role`
    - `src/transcendence_memory/bootstrap/models.py` contains `same_machine`
    - `src/transcendence_memory/bootstrap/models.py` contains `split_machine`
    - `src/transcendence_memory/bootstrap/models.py` contains `schema_version`
  </acceptance_criteria>
  <done>Bootstrap contracts are typed and later plans can build on stable enums and models.</done>
</task>

<task type="auto">
  <name>Task 3: Add the initial CLI app stub that later plans can extend</name>
  <files>src/transcendence_memory/cli.py, src/transcendence_memory/bootstrap/models.py</files>
  <read_first>src/transcendence_memory/cli.py, src/transcendence_memory/bootstrap/models.py, .planning/phases/01-guided-bootstrap-and-safe-configuration/01-CONTEXT.md</read_first>
  <action>Create `src/transcendence_memory/cli.py` with a Typer application named `app`. Add placeholder subcommands or command groups for `init`, `config`, and `doctor` so later plans can extend real behavior without changing the entry surface. Include a top-level help description that identifies the tool as a bootstrap/configuration CLI and not yet a backend runtime controller. Ensure the file imports the typed bootstrap models where appropriate rather than hard-coding role strings.</action>
  <verify>rg -n "Typer|init|config|doctor|bootstrap" src/transcendence_memory/cli.py</verify>
  <acceptance_criteria>
    - `src/transcendence_memory/cli.py` contains `Typer`
    - `src/transcendence_memory/cli.py` contains `init`
    - `src/transcendence_memory/cli.py` contains `config`
    - `src/transcendence_memory/cli.py` contains `doctor`
  </acceptance_criteria>
  <done>The CLI entry surface exists and later plans can extend it without renaming commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "transcendence_memory\\.cli:app" pyproject.toml` succeeds
- [ ] `rg -n "class Role|class BootstrapState" src/transcendence_memory/bootstrap/models.py` succeeds
- [ ] `rg -n "Typer|doctor" src/transcendence_memory/cli.py` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No backend runtime dependencies or runtime code are introduced
- The repository contains a stable CLI and bootstrap model foundation for later Phase 1 plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-guided-bootstrap-and-safe-configuration/01-01-SUMMARY.md`
</output>
